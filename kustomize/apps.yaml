apiVersion: kustomize.toolkit.fluxcd.io/v1beta1
kind: Kustomization
metadata:
  name: environments
  namespace: flux-system
spec:
  interval: 1m
#  dependsOn:
#    - name: infrastucture
  sourceRef:
    kind: GitRepository
    name: flux-system
  prune: true
  path: ./environments
  validation: client
